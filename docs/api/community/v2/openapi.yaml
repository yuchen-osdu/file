openapi: 3.1.0
info:
  title: File Service
  description: "This service allows users to manage files on the  Data Platform. File\
    \ Management includes uploads, downloads and creation of metadata record for the\
    \ file."
  contact:
    name: OSDU Data Platform Team
    email: dps@OSDU.org
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html
  version: 2.0.0
servers:
- url: /api/file/
security:
- Authorization: []
tags:
- name: file-admin-api
  description: File Admin API
- name: file-metadata-api
  description: File Metadata API
- name: file-location-api
  description: File Location API
- name: health-check-api
  description: Health Check API
- name: file-delivery-api
  description: File Delivery API
- name: info
  description: Version info endpoint
paths:
  /v2/files/revokeURL:
    post:
      tags:
      - file-admin-api
      summary: Revoked the Signed URL
      description: "This API Revokes the Signed URLs based on the given request parameters.\
        \ For example: for the given storage account. <p> **Required roles**: `service.file.admin`.</p>"
      operationId: revokeURL
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties:
                type: string
        required: true
      responses:
        "204":
          description: Revoked URLs successfully.
        "400":
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "403":
          description: User not authorized to perform the action
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "502":
          description: Bad Gateway
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "503":
          description: Service Unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
      security:
      - Authorization: []
  /v2/files/metadata:
    post:
      tags:
      - file-metadata-api
      summary: Creates a metadata for a file
      description: "This API creates a metadata record for a file that is already\
        \ uploaded. The Metadata is linked to the file via `FileSource` provided in\
        \ the request body. <p> If `FileSource` attribute is missing in the request\
        \ body or there is no file present, then the request fails with an error.\
        \ </p><p> When metadata is successfully updated in the system, it returns\
        \ the `Id` of the file metadata record. </p><p> **Required roles**: `service.file.editors`.\
        \ Users added to groups `users.datalake.editors`, `users.datalake.admins`,\
        \ `users.datalake.ops` would be added to group `service.file.editors` by default.</p>"
      operationId: postFilesMetadata
      parameters:
      - name: data-partition-id
        in: header
        description: Tenant Id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FileMetadata"
        required: true
      responses:
        "201":
          description: Metadata created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileMetadataResponse"
        "400":
          description: Bad user input. Mandatory fields missing or unacceptable value
            passed to API
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "403":
          description: User not authorized to perform the action
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "404":
          description: Record Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "502":
          description: Bad Gateway
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "503":
          description: Service Unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
      security:
      - Authorization: []
  /v2/readiness_check:
    get:
      tags:
      - health-check-api
      summary: Readiness Check endpoint
      description: For deployment available public `/readiness_check` endpoint.
      operationId: readinessCheck
      parameters:
      - name: data-partition-id
        in: header
        description: Tenant Id
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: string
  /v2/liveness_check:
    get:
      tags:
      - health-check-api
      summary: Liveness Check endpoint
      description: For deployment available public `/liveness_check` endpoint.
      operationId: livenessCheck
      parameters:
      - name: data-partition-id
        in: header
        description: Tenant Id
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: string
  /v2/info:
    get:
      tags:
      - info
      summary: Version info
      description: "For deployment available public `/info` endpoint, which provides\
        \ build and git related information."
      operationId: info
      parameters:
      - name: data-partition-id
        in: header
        description: Tenant Id
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Version info.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VersionInfo"
  /v2/files/{id}/metadata:
    get:
      tags:
      - file-metadata-api
      summary: Gets metadata record for the given id
      description: "Gets the latest version of File metadata record identified by\
        \ the given id. <p> **Required roles**: `service.file.editors`. Users added\
        \ to groups `users.datalake.editors`, `users.datalake.admins`, `users.datalake.ops`\
        \ would be added to group `service.file.editors` by default.</p>"
      operationId: getFileMetadataById
      parameters:
      - name: id
        in: path
        description: File metadata record Id.
        required: true
        schema:
          type: string
      - name: data-partition-id
        in: header
        description: Tenant Id
        required: true
        schema:
          type: string
      responses:
        "200":
          description: A successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordVersion"
        "400":
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "403":
          description: User not authorized to perform the action
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "404":
          description: Record Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "502":
          description: Bad Gateway
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "503":
          description: Service Unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
      security:
      - Authorization: []
    delete:
      tags:
      - file-metadata-api
      summary: Deletes metadata record & file associated with that record for the
        given id
      description: "Deletes the File metadata record identified by the given id and\
        \ file associated with that metadata record. <p> **Required roles**: `users.datalake.editors`\
        \  or `users.datalake.admins`.</p>"
      operationId: deleteFileMetadataById
      parameters:
      - name: id
        in: path
        description: File metadata record Id.
        required: true
        schema:
          type: string
      - name: data-partition-id
        in: header
        description: Tenant Id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: Record deleted successfully.
        "400":
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "403":
          description: User not authorized to perform the action
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "404":
          description: Record Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "502":
          description: Bad Gateway
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "503":
          description: Service Unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
      security:
      - Authorization: []
  /v2/files/{id}/downloadURL:
    get:
      tags:
      - file-delivery-api
      summary: Gets a URL to download the file
      description: "Gets a URL for downloading the file associated with the unique\
        \ `id`.By default, the download URL is valid for `1 Hour` and it is `7 Days`\
        \ maximum.<p> **Required roles**: `service.file.viewers`. Users added to groups\
        \ `users.datalake.viewers`,`users.datalake.editors`, `users.datalake.admins`,\
        \ `users.datalake.ops` would be added to group `service.file.viewers` by default.</p>"
      operationId: downloadURL
      parameters:
      - name: id
        in: path
        description: File Metadata record Id.
        required: true
        schema:
          type: string
      - name: expiryTime
        in: query
        description: "The Time for which Signed URL to be valid. Accepted Regex patterns\
          \ are \"^[0-9]+M$\", \"^[0-9]+H$\", \"^[0-9]+D$\" denoting Integer values\
          \ in Minutes, Hours, Days respectively. In absence of this parameter the\
          \ URL would be valid for 1 Hour."
        required: false
        schema:
          type: string
        example: 5M
      - name: data-partition-id
        in: header
        description: Tenant Id
        required: true
        schema:
          type: string
      responses:
        "200":
          description: A successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DownloadUrlResponse"
        "400":
          description: Bad user input. Mandatory fields missing or unacceptable value
            passed to API
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "403":
          description: User not authorized to perform the action
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "404":
          description: Record Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "502":
          description: Bad Gateway
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "503":
          description: Service Unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
      security:
      - Authorization: []
  /v2/files/uploadURL:
    get:
      tags:
      - file-location-api
      summary: Get a location in Landing Zone to upload a file.
      description: "Gets a temporary signed URL to upload a file (Service does not\
        \ upload the file by itself, User needs to use this URL to upload the file).\
        \ The generated URL is time bound and by default expires by 7 days maximum.\
        \ <p> User will receive a FileSource in the response.This is the relative\
        \ path where the uploaded file will persist. Once the file is uploaded, FileSource\
        \ can then be used to post metadata of the file.</p> <p> **Required roles**:\
        \ `service.file.editors`. Users added to groups `users.datalake.editors`,\
        \ `users.datalake.admins`, `users.datalake.ops` would be added to group `service.file.editors`\
        \ by default.</p>"
      operationId: getLocationFile
      parameters:
      - name: expiryTime
        in: query
        description: "The Time for which Signed URL to be valid. Accepted Regex patterns\
          \ are \"^[0-9]+M$\", \"^[0-9]+H$\", \"^[0-9]+D$\" denoting Integer values\
          \ in Minutes, Hours, Days respectively. In absence of this parameter the\
          \ URL would be valid for 1 Hour."
        required: false
        schema:
          type: string
        example: 5M
      - name: data-partition-id
        in: header
        description: Tenant Id
        required: true
        schema:
          type: string
      responses:
        "200":
          description: File location
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LocationResponse"
        "400":
          description: Bad user input. Mandatory fields missing or unacceptable value
            passed to API
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "403":
          description: User not authorized to perform the action
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "404":
          description: Record Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "502":
          description: Bad Gateway
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
        "503":
          description: Service Unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppError"
      security:
      - Authorization: []
components:
  schemas:
    AppError:
      type: object
      description: Standard error response returned by OSDU services.
      properties:
        code:
          type: integer
          format: int32
          description: HTTP status code.
          example: 400
        reason:
          type: string
          description: Short error reason.
          example: Bad Request
        message:
          type: string
          description: Detailed error message.
          example: Invalid record format.
    Acl:
      type: object
      description: Access control lists defining which groups can view or own a record.
      properties:
        viewers:
          type: array
          description: List of valid groups which will have view/read privileges over
            the record.
          items:
            type: string
            example: data.default.viewers@opendes.contoso.com
            pattern: "^data\\.[a-zA-Z0-9_+&*-]+(?:\\.[a-zA-Z0-9_+&*-]+)*@(?:[a-zA-Z](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\\\
              .)+[a-zA-Z](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?$"
        owners:
          type: array
          description: List of valid groups which will have write privileges over
            the record.
          items:
            type: string
            example: data.default.owners@opendes.contoso.com
            pattern: "^data\\.[a-zA-Z0-9_+&*-]+(?:\\.[a-zA-Z0-9_+&*-]+)*@(?:[a-zA-Z](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\\\
              .)+[a-zA-Z](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?$"
      required:
      - owners
      - viewers
    Ancestry:
      type: object
      description: A named list of entities in the data lake as a dictionary item.
      properties:
        parents:
          type: array
          description: An array of one or more entity references in the data lake.
          items:
            type: string
    DatasetProperties:
      type: object
      description: Dataset Properties
      properties:
        FileSourceInfo:
          $ref: "#/components/schemas/FileSourceInfo"
      required:
      - FileSourceInfo
    FileData:
      type: object
      description: The file data container containing all necessary details of the
        file record
      properties:
        Name:
          type: string
          description: "An optional name of the dataset, e.g. a user friendly file\
            \ or file collection name."
        Description:
          type: string
          description: "An optional, textual description of the dataset."
        TotalSize:
          type: string
          description: Total size of the dataset in bytes; for files it is the same
            as declared in FileSourceInfo.FileSize or the sum of all individual files.
            Implemented as string. The value must be convertible to a long integer
            (sizes can become very large).
          pattern: "^[0-9]+$"
        EncodingFormatTypeID:
          type: string
          description: Encoding Format Type ID
          pattern: "^srn:<namespace>:reference-data\\\\/EncodingFormatType:[^:]+:[0-9]*$"
        SchemaFormatTypeID:
          type: string
          description: Schema Format Type ID
          pattern: "^srn:<namespace>:reference-data\\\\/SchemaFormatType:[^:]+:[0-9]*$"
        ResourceHomeRegionID:
          type: string
          description: Resource Home Region ID
        ResourceHostRegionIDs:
          type: array
          description: Resource Host Region IDs
          items:
            type: string
        ResourceCurationStatus:
          type: string
          description: Resource Curation Status
        ResourceLifecycleStatus:
          type: string
          description: Resource Lifecycle Status
        ResourceSecurityClassification:
          type: string
          description: Resource Security Classification
        Source:
          type: string
          description: Source
        DatasetProperties:
          $ref: "#/components/schemas/DatasetProperties"
        ExistenceKind:
          type: string
          description: Existence Kind
        Endian:
          type: string
          description: Endianness of binary value. Enumeration- \BIG\ \LITTLE\.  If
            absent applications will need to interpret from context indicators.
          enum:
          - BIG
          - LITTLE
        Checksum:
          type: string
          description: MD5 checksum of file bytes - a 32 byte hexadecimal number
          pattern: "^[0-9a-fA-F]32}$"
        ExtensionProperties:
          type: object
          additionalProperties:
            type: object
          description: File DMS Extension Properties
      required:
      - DatasetProperties
    FileMetadata:
      type: object
      description: File metadata record.
      properties:
        id:
          type: string
          description: Unique identifier generated by the system for the file metadata
            record.
        kind:
          type: string
          description: |-
            Kind of data being ingested. Must follow the naming
              convention:data-Partition-Id}:dataset-name}:record-type}:version}.
          example: osdu:wks:dataset--File.Generic:1.0.0
        legal:
          $ref: "#/components/schemas/Legal"
        data:
          $ref: "#/components/schemas/FileData"
        ancestry:
          $ref: "#/components/schemas/Ancestry"
        meta:
          type: array
          items:
            type: object
            additionalProperties:
              type: object
        tags:
          type: object
          additionalProperties:
            type: string
        acl:
          $ref: "#/components/schemas/Acl"
      required:
      - acl
      - data
      - kind
      - legal
    FileSourceInfo:
      type: object
      description: File Source Info
      properties:
        FileSource:
          type: string
          description: Relative file path for the data in the file
          minLength: 1
        PreloadFilePath:
          type: string
          description: File system path to the data file as it existed before loading
            to the data platform
        PreloadFileCreateUser:
          type: string
          description: "Optional user name or reference, who created the file prior\
            \ to up-loading to the platform."
        PreloadFileCreateDate:
          type: string
          description: Optional create date and time of the file prior to uploading
            to the platform.
        PreloadFileModifyUser:
          type: string
          description: "Optional user name or reference, who last modified the file\
            \ prior to up-loading to the platform."
        PreloadFileModifyDate:
          type: string
          description: Optional last modified date and time of the file prior to up-loading
            to the platform.
        Name:
          type: string
          description: user-friendly file name.
        FileSize:
          type: string
          description: Length of file in bytes. Implemented as string. The value must
            be convertible to a long integer (sizes can become very large).
        EncodingFormatTypeID:
          type: string
          description: Encoding Format Type ID
          pattern: "^srn:<namespace>:reference-data\\\\/EncodingFormatType:[^:]+:[0-9]*$"
        Checksum:
          type: string
          description: MD5 checksum of file bytes - a 32 byte hexadecimal number
          pattern: "^[0-9a-fA-F]32}$"
        ChecksumAlgorithm:
          type: string
          description: "The name of the checksum algorithm e.g. MD5, SHA-256."
      required:
      - FileSource
    Legal:
      type: object
      properties:
        legaltags:
          type: array
          description: List of legal tag names associated with this record.
          items:
            type: string
            example: opendes-Default-Legal-Tag
          minItems: 1
          uniqueItems: true
        otherRelevantDataCountries:
          type: array
          description: ISO 3166-1 alpha-2 country codes of other relevant data countries.
          items:
            type: string
            example: US
          minItems: 1
          uniqueItems: true
        status:
          type: string
          description: "Compliance status of the legal tags, assigned by the server."
          enum:
          - compliant
          - incompliant
          example: compliant
          readOnly: true
      required:
      - legaltags
      - otherRelevantDataCountries
    FileMetadataResponse:
      type: object
      description: File metadata Response
      properties:
        id:
          type: string
          description: Unique identifier generated by the system for the file metadata
            record.
    ConnectedOuterService:
      type: object
      description: Name and version of an external service that this OSDU service
        connects to.
      properties:
        name:
          type: string
          description: Name of the connected external service.
          example: storage
        version:
          type: string
          description: Version of the connected external service.
          example: 2.0.0
    FeatureFlagState:
      type: object
      description: State of a feature flag for a given partition and source.
      properties:
        name:
          type: string
          description: Name of the feature flag.
          example: collaborations-api
        enabled:
          type: boolean
          description: Whether the feature flag is currently enabled.
          example: true
        partition:
          type: string
          description: Data partition this flag applies to.
          example: opendes
        source:
          type: string
          description: Source configuration that provided this flag state.
          example: runtime
    VersionInfo:
      type: object
      description: Version and build information for an OSDU service.
      properties:
        groupId:
          type: string
          description: Maven group ID of the service artifact.
          example: org.opengroup.osdu
        artifactId:
          type: string
          description: Maven artifact ID of the service.
          example: os-search
        version:
          type: string
          description: Semantic version of the deployed service.
          example: 0.27.0
        buildTime:
          type: string
          description: ISO 8601 timestamp of the build.
          example: 2021-01-26T02:24:13.843Z
        branch:
          type: string
          description: Source control branch from which the service was built.
          example: main
        commitId:
          type: string
          description: Short commit SHA of the build.
          example: "7777777"
        commitMessage:
          type: string
          description: Commit message of the build.
          example: initial commit
        connectedOuterServices:
          type: array
          description: List of external services this service depends on.
          items:
            $ref: "#/components/schemas/ConnectedOuterService"
        featureFlagStates:
          type: array
          description: "Current feature flag states for this partition, if any."
          items:
            $ref: "#/components/schemas/FeatureFlagState"
    RecordVersion:
      type: object
      description: Metadata record
      properties:
        id:
          type: string
          description: Unique identifier generated by the system for the file metadata
            record.
        kind:
          type: string
          description: |-
            Kind of data being ingested. Must follow the naming
              convention:data-Partition-Id}:dataset-name}:record-type}:version}.
          example: osdu:wks:dataset--File.Generic:1.0.0
        legal:
          $ref: "#/components/schemas/Legal"
        data:
          $ref: "#/components/schemas/FileData"
        ancestry:
          $ref: "#/components/schemas/Ancestry"
        version:
          type: integer
          format: int64
          description: The version number of this OSDU resource; set by the framework.
          example: 1831253916104085
        meta:
          type: array
          items:
            type: object
            additionalProperties:
              type: object
        tags:
          type: object
          additionalProperties:
            type: string
        acl:
          $ref: "#/components/schemas/Acl"
      required:
      - acl
      - data
      - kind
      - legal
    DownloadUrlResponse:
      type: object
      description: Download URL response
      properties:
        SignedUrl:
          type: string
          description: Signed URL
    LocationResponse:
      type: object
      properties:
        FileID:
          type: string
        Location:
          type: object
          additionalProperties:
            type: string
  securitySchemes:
    Authorization:
      type: http
      name: Authorization
      in: header
      scheme: bearer
      bearerFormat: Authorization
